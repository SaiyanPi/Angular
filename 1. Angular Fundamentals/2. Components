COMPONENTS
    - create a component
    - register it in a module
    - add an element in an HTML markup

    Inspect & Run AngularFundamentals project to see implementation.
    -> courses.component.ts is our newly created component.
